"use server";
import Import from "./components/import";

type ImportPageProps = object;

// eslint-disable-next-line no-empty-pattern
export default async function ImportPage({}: ImportPageProps){
  return <Import />;
};
